#!/bin/bash
mkdir 0pct_refine
for i in $(ls experiments/scenarios/aha/softobs_0pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 0pct_refine
mkdir 10pct_refine
for i in $(ls experiments/scenarios/aha/softobs_10pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 10pct_refine
mkdir 20pct_refine
for i in $(ls experiments/scenarios/aha/softobs_20pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 20pct_refine
mkdir 30pct_refine
for i in $(ls experiments/scenarios/aha/softobs_30pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 30pct_refine
mkdir 40pct_refine
for i in $(ls experiments/scenarios/aha/softobs_40pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 40pct_refine
mkdir 50pct_refine
for i in $(ls experiments/scenarios/aha/softobs_50pct/*.scenario);
do
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 10
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 15
   ./bin/sample -scenario $i -gui disable -quality high  -clustersize 20
   ./bin/sample -scenario $i -gui disable -quality low  -clustersize 20
done
mv results_* 50pct_refine
